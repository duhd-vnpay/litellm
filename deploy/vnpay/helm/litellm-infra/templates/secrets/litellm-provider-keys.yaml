# Placeholder — replace with SealedSecret or create manually:
#   kubectl create secret generic litellm-provider-keys \
#     --from-literal=ANTHROPIC_API_KEY=<ANTHROPIC-KEY> \
#     --from-literal=OPENAI_API_KEY=<OPENAI-KEY> \
#     --from-literal=REDIS_PASSWORD=<GENERATED-PASSWORD> \
#     -n {{ .Values.global.namespace }}
#
# Add more provider keys as needed:
#     --from-literal=GEMINI_API_KEY=<KEY> \
#     --from-literal=DEEPSEEK_API_KEY=<KEY> \
#     --from-literal=GROQ_API_KEY=<KEY>
