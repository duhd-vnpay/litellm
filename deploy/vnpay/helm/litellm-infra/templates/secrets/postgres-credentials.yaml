# Placeholder — replace with SealedSecret or create manually:
#   kubectl create secret generic litellm-postgres-credentials \
#     --from-literal=username=litellm \
#     --from-literal=password=<PASSWORD> \
#     --from-literal=dsn=postgresql://litellm:<PASSWORD>@litellm-postgresql:5432/litellm \
#     -n {{ .Values.global.namespace }}
