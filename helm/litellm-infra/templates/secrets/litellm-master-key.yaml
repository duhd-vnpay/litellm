# Placeholder — replace with SealedSecret or create manually:
#   kubectl create secret generic litellm-master-key \
#     --from-literal=PROXY_MASTER_KEY=sk-litellm-<GENERATED-UUID> \
#     --from-literal=LITELLM_SALT_KEY=<GENERATED-32-CHAR-STRING> \
#     -n {{ .Values.global.namespace }}
